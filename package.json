{
  "name": "ai-powered-backend",
  "private": true,
  "version": "0.1.0",
  "description": "AI-Powered Backend with Strapi",
   "scripts": {
    "build": "mkdir -p public/uploads && mkdir -p .tmp && strapi build",
    "develop": "mkdir -p public/uploads && mkdir -p .tmp && strapi develop", 
    "start": "mkdir -p public/uploads && mkdir -p .tmp && strapi start",
    "strapi": "strapi"
  },
  "dependencies": {
    "@strapi/strapi": "^4.25.23",
    "@strapi/plugin-users-permissions": "^4.25.23",
    "pg": "^8.11.0",
    "better-sqlite3": "^8.7.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=6.0.0"
  }
}
